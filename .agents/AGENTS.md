# Project Rules

## Git & Deployment Rules
- **Do NOT push code to GitHub automatically** (`git push` is forbidden unless explicitly instructed by the user).
- All code modifications and testing must be done **locally** (`c:\Users\kenkr\OneDrive\Desktop\BotDichTuVaNote\dichvanotechu2`).
- Only run `git push` when the user explicitly requests to update/push code to GitHub (e.g., "cập nhật code lên GitHub", "push code", etc.).
